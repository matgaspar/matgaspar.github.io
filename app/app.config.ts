// Nuxt UI v4 theme — must live in the Nuxt 4 srcDir (app/) to take effect.
// Cohesive, analogous palette: indigo primary with a sky secondary on slate.
export default defineAppConfig({
  ui: {
    colors: {
      primary: 'blue',
      secondary: 'cyan',
      neutral: 'slate',
    },
  },
})
